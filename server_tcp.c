#include "common.h"

int main() {
    // declaration des variables pour la socket
    int sockfd;
    struct sockaddr_in serv_addr;
    
    printf("demarrage du serveur mastermind...\n");

    // etape 1 : creation de la socket tcp
    // a faire

    // etape 2 : preparation de l'adresse du serveur (bind)
    // a faire

    // etape 3 : mise en ecoute (listen)
    // a faire

    // boucle principale pour accepter les clients
    //

    return 0;
}