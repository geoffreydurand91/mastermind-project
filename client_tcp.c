#include "common.h"

int main() {
    // declaration des variables
    int sockfd;
    struct sockaddr_in serv_addr;

    printf("demarrage du client mastermind...\n");

    // etape 1 : creation de la socket tcp
    // a faire

    // etape 2 : configuration de l'adresse du serveur a contacter
    // a faire

    // etape 3 : connexion au serveur (connect)
    // a faire

    // etape 4 : boucle de jeu (envoi proposition, reception reponse)
    // a faire

    return 0;
}